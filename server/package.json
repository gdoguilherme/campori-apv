{
  "name": "campori-upload-server",
  "version": "3.0.0",
  "description": "Servidor de upload para o Campori APV — Google Drive via service account",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "googleapis": "^140.0.1",
    "multer": "^1.4.5-lts.1"
  }
}
